docker rm: Removes one or more containers. ================ docker rm webserver2 or container id

to delete all stopped containers 
docker rm $(docker ps -aq)

to forcefully delete all containers running or stopped
docker rm -f $(docker ps -aq)
